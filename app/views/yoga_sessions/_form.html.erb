<%= form_for(@yoga_session) do |f| %>
  <% if @yoga_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yoga_session.errors.count, "error") %> prohibited this yoga_session from being saved:</h2>

      <ul>
      <% @yoga_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session_date, "Date" %><br>
    <%= f.date_select :session_date %>
  </div>
  <div class="field">
    <%= f.label :session_time, "Time" %><br>
    <%= f.time_field(:session_time) %>
  </div>
  <div class="field">
    <%= f.label :yoga_class_id, "Yoga Class" %><br>
    <%= f.collection_select :yoga_class_id, YogaClass.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit"%>
  </div>
<% end %>
